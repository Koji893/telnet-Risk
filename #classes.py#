import asyncio
import random
import allowed.py
class Game:
    def __init__(self):
        self.players = []
        self.status = 0 
        self.state = "selecting Turn order"
        self.turns= []
        self.bindReason = None
        self.rolls = {}
    def add_player(self,player):
        self.players.append(player)
    def remove_player(self,player):
        if player in self.players:
            self.players.remove(player)
    def passrole(self,rolls,players):
        self.rolls[player] = rolls
    def get_all_players(self):
        return self.players
    async def rollProcessor(self,partcipating_Players):
        while True:
            if set(self.rolls.keys()) == set(participatingPlayers):
                return True
    async def turnOrder():
        self.bindReason= "Rolling for Turn Order"
        self.bind_players()
        
    def bind_player(self,player):
        player.bound=True
    def bind_players():
        for player in self.players:
            self.bind(player)
    def start(self):
        self.turnOrder()
    

class Player:
    Players = []
    def __init__(self,name,client):
        self.name = name
        self.client = client
        self.bound=False
class Dice:
    def __init__(self):
        self.rolls = []
        self.value = None
    def roll(self,quantity):
        self.rolls = {}
        for i in range(quantity):
            value= randint(1,6)
            self.rolls.append(value)
        self.rolls.sort(reverse=True)
        return self.rolls
